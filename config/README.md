# Configuration templates

Copy these files when setting up the project:

| File | Copy to |
|------|---------|
| `backend.env.example` | `../backend/.env` |
| `frontend.env.example` | `../frontend/.env` |

Never commit real `.env` files with secrets to git.
