# Frontend - Smart College Event Management

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start app:
   ```bash
   npm start
   ```

The frontend uses a proxy to `http://localhost:5000` for API requests.
